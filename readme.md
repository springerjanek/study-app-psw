# BACKEND SETUP:

# .env:

DB_HOST=''
DB_USER=''
DB_PASSWORD=''
DB_NAME=''
DB_PORT=''
JWT_SECRET=''

# TLS CERTIFICATE

link your certifacte files in `app.js` and `vite.config.js` at frontend

# SQL SETUP:

/backend/schema.sql
